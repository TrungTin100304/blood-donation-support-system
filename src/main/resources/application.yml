spring:
  application:
    name: blood_system
  datasource:
    url: jdbc:mysql://${MYSQL_URL:localhost}:${MYSQL_PORT:3307}/${MYSQL_DATABASE:blood_system}
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_ROOT_PASSWORD:admin123}
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: "${GOOGLE_CLIENT_ID:1061123444145-ga7gecn3snrnrvkg79fvp759n5iq8ibd.apps.googleusercontent.com}"
            client-secret: "${GOOGLE_CLIENT_SECRET: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}"
            redirect-uri: "${GOOGLE_REDIRECT_URI:http://localhost:3000/auth/social/callback/google}"
            auth-uri: "https://accounts.google.com/o/oauth2/v2/auth"
            token-uri: "https://www.googleapis.com/oauth2/v3/token"
            user-info-uri: "https://www.googleapis.com/oauth2/v3/userinfo"
            scope:
              - email
              - public_profile
  mail:
    host: smtp.gmail.com
    port: 587
    username: bloodsystem123@gmail.com
    password: hxmz gmzi qfrf qzqa
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    jackson:
      time-zone: Asia/Ho_Chi_Minh


  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB



  upload:
    path: ./upload

jwt:
  secret: va9TVkfZSFlAI4ifEoV6mjSHkZSYbaIJF8L3Y7douCM=
  expiration: 86400

buildCommand: ./mvnw clean install


# application.yml
logging:
  level:
    org.springframework.web: DEBUG
    org.springframework.http.converter: DEBUG

